name: One-time - Deploy unified auth linking

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-dispatch-auth-linking.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Dispatch Firebase workflow
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/firebase-deploy.yml/dispatches" \
            -d '{"ref":"main"}'
          echo "firebase-deploy workflow_dispatch sent"
