name: React migration - runtime catalog map v2

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/react-runtime-catalog-map-v2.yml'
      - 'scripts/patch-react-runtime-catalog-map-v2.py'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: react-runtime-catalog-map-v2
  cancel-in-progress: true

jobs:
  patch-build-push:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      PRIVATE_REPO: ${{ secrets.PRIVATE_REPO }}
      PRIVATE_REPO_PAT: ${{ secrets.PRIVATE_REPO_PAT || secrets.PERSONAL_ACCESS_TOKEN || secrets.PAT || secrets.GH_PAT || secrets.GITHUB_PAT }}
    steps:
      - name: Verify private repository access
        shell: bash
        run: |
          set -euo pipefail
          test -n "$PRIVATE_REPO"
          test -n "$PRIVATE_REPO_PAT"

      - name: Checkout automation repo
        uses: actions/checkout@v7
        with:
          path: automation
          persist-credentials: false

      - name: Checkout React migration
        uses: actions/checkout@v7
        with:
          repository: ${{ secrets.PRIVATE_REPO }}
          token: ${{ secrets.PRIVATE_REPO_PAT || secrets.PERSONAL_ACCESS_TOKEN || secrets.PAT || secrets.GH_PAT || secrets.GITHUB_PAT }}
          ref: react-migration
          path: source
          fetch-depth: 0
          persist-credentials: true

      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: source/frontend-react/package-lock.json

      - name: Assert expected migration base
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C source rev-parse HEAD)" = "08055dbb79648261b3b767cf10a2ec7c87114c33"

      - name: Apply runtime Catalog and Map patch
        run: python3 automation/scripts/patch-react-runtime-catalog-map-v2.py

      - name: Build React migration
        working-directory: source/frontend-react
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm run build

      - name: Validate runtime bundle and source invariants
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re
          import subprocess

          root = Path('source/frontend-react')
          generated = (root / 'src/generated/tourism-data-core.ts').read_text(encoding='utf-8')
          ids = re.findall(r'"id"\s*:\s*"([^"]+)"', generated)
          print(f'generated tourism ids: {len(ids)}')
          assert len(ids) == 52, len(ids)
          assert len(set(ids)) == 52
          assert 'turismo-isola-dino' in ids
          assert 'turismo-catanzaro-centro' in ids

          catalog = (root / 'src/pages/CatalogPage.tsx').read_text(encoding='utf-8')
          map_page = (root / 'src/pages/MapPage.tsx').read_text(encoding='utf-8')
          for token in ['data-react-catalog-revision="20260912-catalog-map-v2"','id="result-count"','id="results"','id="category"','id="province"','id="city"']:
              assert token in catalog, token
          for token in ['data-react-map-revision="20260912-catalog-map-v2"','[mapReady, filtered','id="verified" hidden','id="map-results" hidden','globalThis as unknown as { L?: typeof import("leaflet") }']:
              assert token in map_page, token

          assets = list((root / 'dist/assets').glob('*.js'))
          bundle = '\n'.join(p.read_text(encoding='utf-8', errors='ignore') for p in assets)
          marker = '20260912-catalog-map-v2'
          print(f'revision marker in client bundle: {marker in bundle}')
          assert marker in bundle
          tourism_chunks = [p for p in assets if p.name.startswith('tourism-data-core-')]
          assert tourism_chunks, 'tourism-data-core client chunk missing'
          tourism_size = max(p.stat().st_size for p in tourism_chunks)
          print(f'tourism client chunk bytes: {tourism_size}')
          assert tourism_size > 15000, tourism_size
          assert any(p.name.startswith('CatalogPage-') for p in assets)
          assert any(p.name.startswith('MapPage-') for p in assets)

          check = subprocess.run(['git','-C','source','diff','--check'], text=True, capture_output=True)
          if check.stdout: print(check.stdout)
          if check.stderr: print(check.stderr)
          assert check.returncode == 0, 'git diff --check failed'
          print('RUNTIME_BUNDLE_VALIDATION_GREEN')
          PY

      - name: Commit and push only after green build
        shell: bash
        run: |
          set -euo pipefail
          git -C source config user.name 'CalabriaVera Automation'
          git -C source config user.email 'actions@users.noreply.github.com'
          git -C source add frontend-react/src/pages/CatalogPage.tsx frontend-react/src/pages/MapPage.tsx
          if git -C source diff --cached --quiet; then
            echo 'No staged changes'
            exit 0
          fi
          git -C source commit -m 'fix: render current catalog data and map markers'
          git -C source push origin HEAD:react-migration
